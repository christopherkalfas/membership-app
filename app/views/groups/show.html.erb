<h1><%= @group.name %></h1>
<h3><%=@group.owner.name%></h3>
<h4>Members:</h4>
<ul>
  <% @group.users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>

<%= link_to("Add new Member", new_membership_path)%>

